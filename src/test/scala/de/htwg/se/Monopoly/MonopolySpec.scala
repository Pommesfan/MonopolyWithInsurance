package de.htwg.se.Monopoly

import java.io.ByteArrayInputStream

import org.scalatest._
import org.junit.runner.RunWith
import org.scalatestplus.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class MonopolySpec extends WordSpec with Matchers {
  /*
  "Monopoly" when {
    "new" should {
      val monopoly = Monopoly
      "start and exit game" in {
        val in = new ByteArrayInputStream("e".getBytes)
        Console.withIn(in) {
          monopoly.main(Array[String]())
        }
      }
      "start game and set player" in {
        val in = new ByteArrayInputStream("p player1 and player2\nd\nN\ne".getBytes)
        Console.withIn(in) {
          monopoly.main(Array[String]())
        }
      }
      "start and help" in {
        val in = new ByteArrayInputStream("help\nHallo\ne".getBytes)
        Console.withIn(in) {
          monopoly.main(Array[String]())
        }
      }
    }
  }*/
}
